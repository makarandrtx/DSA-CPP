#include <iostream>
using namespace std;
int main() {

    cout << "Following are the comparisons bet/n Linked List and array";
    cout << "Linked List doesn't occupy a contiguous memory block, hence it's value can be altered during runtime.";
    cout << "A dynamically allocated array also allocates memory at runtime.";
    cout << "Inserting a new element in an array of elements is expensive because room has to be created for the new elements and to create room existing elements have to be shifted. ";
    return 0;
}